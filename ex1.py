"""
Write the function SwapEl, that takes as input arguments a list and two numbers x and y, and swaps the elements of the list at positions x and y. If it is not possible to swap
the numbers, it returns False.

Example:
Input: [1,2,3,4,5], 0, 4
Output: [5,2,3,4,1]

Input: [1,2,3], 1, 6
Output: False


"""

def SwapEl(input_list, x, y):
    # This variable will contain the new list or the value False
    result = None

    # write your instructions here

    return result



# Write your test code inside this statement
if __name__ == "__main__":
    SwapEl([],0,0)